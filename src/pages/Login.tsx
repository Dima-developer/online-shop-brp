import React, { FC } from 'react';
import { Box } from '@mui/system';

const LoginPage: FC = () => {
  return (
    <Box component='div'>
      <p>Login page</p>
    </Box>
  );
};
export default LoginPage;
